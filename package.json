{
  "name": "ai-driven-testing",
  "version": "1.0.0",
  "description": "Playwright demo repo, CLI variant: the playwright-cli skill instead of the MCP subagents.",
  "private": true,
  "scripts": {
    "playwright:install": "playwright install --with-deps",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "keywords": [
    "playwright",
    "testing",
    "ai",
    "agents",
    "mcp"
  ],
  "author": "Martin Krištof",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/apify-projects/ai-driven-testing.git"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/cli": "0.1.19",
    "@playwright/test": "1.64.0-alpha-2026-09-04",
    "@types/node": "^24.10.1",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.3"
  }
}
